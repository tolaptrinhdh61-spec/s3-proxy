/**
 * src/index.js — Bootstrap entry point
 * Full implementation in T6. This stub satisfies Docker build for T1.
 */
import config from './config.js'

console.log('[s3-proxy] config loaded, INSTANCE_ID:', config.INSTANCE_ID)
console.log('[s3-proxy] Waiting for full implementation in T6...')
